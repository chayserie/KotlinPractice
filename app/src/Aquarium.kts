import java.util.*

    fun fitMoreFish(tankSize: Int,
                    currentFish: Int,
                    fishSize: Int = 2,
                    hasDecoration: Boolean = true
                    )
